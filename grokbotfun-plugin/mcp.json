{
  "mcpServers": {
    "grokbotfun": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "grokbotfun"],
      "env": {
        "PUMPFUN_PRIVATE_KEY": "${PUMPFUN_PRIVATE_KEY}",
        "SOLANA_RPC_URL": "${SOLANA_RPC_URL}"
      }
    }
  }
}
